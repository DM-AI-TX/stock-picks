import type { DividendFiltered, ScoredStock } from "./lib/types"; export async function runDetailScoring( candidates: DividendFiltered[] ): Promise<ScoredStock[]> { const scored: ScoredStock[] = candidates.map((stock) => ({ ...stock, priceLevelScore: 0, compositeScore: stock.performanceScore, details: {}, })); return scored.sort((a, b) => b.compositeScore - a.compositeScore); }
